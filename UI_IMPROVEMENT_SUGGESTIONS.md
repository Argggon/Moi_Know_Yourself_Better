# Moi App - UI 改进建议清单

> 文档版本：V1.0  
> 创建日期：2026-08-12  
> 基于版本：V2.3 (iOS 26/27 Liquid Glass)  
> 分析范围：对比 iOS 26/27 原生设计规范

---

## 一、导航与结构层面

### 1. ✅ 导航栏标题显示方式不一致 [已修复]

**当前状态**：三个主视图（Today/Ask/Story）使用自定义 `.topBarLeading` 放置标题，而 Sheet 页面使用标准 `.navigationTitle()`

**问题**：
- 自定义标题破坏了 iOS 26/27 的原生大标题折叠行为
- 用户无法获得系统级的滚动收缩体验
- 与系统设置、邮件等原生 App 行为不一致

**修复状态**：已采用标准 `.navigationTitle()` + `.navigationBarTitleDisplayMode(.large)`

---

### 2. TabBar 没有使用 iOS 26 的最小化行为

**当前状态**：TabBar 固定在底部，滚动时不会收起

**iOS 26/27 原生行为**：长内容滚动时，TabBar 应该可以收起以提供更多内容空间

**建议**：在 `MainTabView` 添加：
```swift
.tabBarMinimizeBehavior(.onScrollDown)  // iOS 26+
```

**备注**：此 API 在 Xcode 27 beta 5 中被移除，需等待 Apple 恢复

---

## 二、视觉设计层面

### 3. 🔴 卡片圆角半径不统一

**当前状态**：
- 设计系统定义圆角为 20pt
- 实际使用：16pt（小卡片）、20pt（大卡片）、12pt（输入框）混用

**iOS 27 Liquid Glass 规范**：统一使用系统级圆角（通常 16pt）

**建议**：
- 统一主要卡片圆角为 16pt
- 在 `DesignSystem` 中定义 `cornerRadiusStandard: CGFloat = 16`
- 小型控件（如日历日期圆圈）保持独立定义

**优先级**：🔴 高

---

### 4. 🔴 空状态图标过于轻薄

**当前状态**：所有空状态使用 `.ultraLight` 字重（48pt）

**iOS 26/27 原生规范**：空状态图标通常使用 `.thin` 或 `.regular`，`.ultraLight` 在大尺寸下显得过于虚弱

**建议**：
- 改用 `.thin` 字重
- 或将尺寸减小到 40pt 并使用 `.ultraLight`

**优先级**：🔴 高

---

## 三、间距与布局层面

### 5. 🔴 列表项垂直间距偏小

**当前状态**：卡片列表间距 12pt

**iOS 26/27 原生规范**：列表项之间通常使用 16pt 或更大间距，提供更好的呼吸感

**建议**：
- 主要卡片列表（Ask 历史、Story 列表）改用 16pt 间距
- 紧凑列表（如 Sparkles 笔记）保持 12pt

**优先级**：🔴 高

---

### 6. 🔴 内容区域水平内边距不一致

**当前状态**：
- 大部分内容使用 `.padding(.horizontal)` （系统默认 16pt）
- 部分手动指定 `36pt`（如空状态文本）

**建议**：
- 统一主内容区域为 20pt（iOS 27 标准）
- 在 `DesignSystem.Metrics` 中定义 `contentHorizontalPadding: CGFloat = 20`

**优先级**：🔴 高

---

### 7. 🔴 Section 标题与内容间距不足

**当前状态**：Section 标题（如 "PAST GUIDANCE"）到内容的间距约 14-16pt

**iOS 原生规范**：Section 标题到内容通常有更大间距（20-24pt）

**建议**：标题下方增加 4-8pt 间距

**优先级**：🔴 高

---

## 四、文字与排版层面

### 8. 🔴 行间距设置不统一

**当前状态**：
- `.body` 文本：6pt、4pt、5pt 混用
- 无明确规范

**建议**：
- 在 `DesignSystem` 中定义标准行间距：
  - `lineSpacingBody: CGFloat = 6`
  - `lineSpacingCompact: CGFloat = 4`
- 统一应用

**优先级**：🔴 高

---

### 9. 🔴 标签文字使用 `.caption2.bold()` 过小

**当前状态**：Section 标题如 "PAST GUIDANCE" 使用 `.caption2.bold()`

**问题**：`.caption2` 在 iOS 27 上可能过小（通常 10-11pt），影响可读性

**建议**：改用 `.caption.bold()` 或 `.footnote.bold()`

**优先级**：🔴 高

---

### 10. 🔴 按钮文字大小不统一

**当前状态**：
- "Learn more" 链接：`.subheadline`
- 主按钮（Onboarding）：`.headline`
- 普通按钮：`.body`

**建议**：制定按钮文字规范：
- 主按钮：`.headline` 或 `.body.semibold()`
- 次要按钮/链接：`.subheadline`

**优先级**：🔴 高

---

## 五、交互与反馈层面

### 11. 🔴 输入框焦点状态无明显反馈

**当前状态**：`MoiInputBar` 的 TextField 聚焦时无视觉变化

**iOS 26/27 原生行为**：焦点状态应有微妙的边框或背景变化

**建议**：
```swift
.glassEffect(isFocused ? .regular.highlighted() : .regular, in: Capsule())
```

**优先级**：🔴 高

---

### 12. 按钮按下状态无视觉反馈

**当前状态**：大部分按钮使用 `.buttonStyle(.plain)`，无按下效果

**建议**：
- 自定义 `GlassButtonStyle` 或使用 `.borderedProminent`
- 或添加 `.scaleEffect` 动画响应点击

**优先级**：🟡 中

---

### 13. 长按、拖拽手势缺失

**当前状态**：所有交互为简单点击

**iOS 26/27 原生交互**：
- 列表项支持长按上下文菜单
- 日历日期可能支持长按快速编辑

**建议**：考虑为历史记录添加 `.contextMenu`（删除、分享等）

**优先级**：🟢 低

---

## 六、iOS 26/27 特性利用

### 14. 🔴 未充分利用 iOS 26 Liquid Glass 效果

**当前状态**：
- 仅在输入组件和关闭按钮使用 `.glassEffect`
- 其他卡片仍使用传统背景（特别是 Ask 结果和 Story 卡片）

**iOS 26/27 设计趋势**：更广泛使用玻璃材质营造层次感

**建议**：
- Ask 结果卡片使用 `.glassEffect(.thin)` 或 `.glassEffect(.regular)`
- Story 卡片列表项考虑使用玻璃效果
- 注意不要过度使用（避免层次混乱）

**优先级**：🔴 高

---

### 15. 未使用 iOS 27 的新排版特性

**iOS 27 新增**：`.typesettingLanguage()` 优化多语言排版

**建议**：为中文内容添加：
```swift
.typesettingLanguage(.init(languageCode: .chinese))
```

**优先级**：🟢 低

---

### 16. 未使用系统级颜色语义

**当前状态**：主色调硬编码为 `Color.blue`

**iOS 设计规范**：应使用 `.tint` 或 `.accent` 让用户自定义

**建议**：保持 `Color.blue` 作为默认，但在 `DesignSystem` 中标注可扩展为自定义主题

**优先级**：🟢 低

---

## 七、无障碍与辅助功能

### 17. 卡片内容缺少无障碍标签

**当前状态**：仅按钮配置了标签，卡片内容依赖系统自动生成

**问题**：复杂布局（HStack + 多个文本）可能生成混乱的阅读顺序

**建议**：
- 为卡片添加 `.accessibilityElement(children: .combine)`
- 关键卡片提供完整 `.accessibilityLabel`

**优先级**：🟡 中

---

### 18. 颜色对比度未验证

**当前状态**：使用系统颜色，但自定义卡片背景未验证

**WCAG 2.1 AA 标准**：文字与背景对比度至少 4.5:1

**建议**：
- 测试 `cardBackground` 深色模式下的对比度
- 特别是灰色半透明元素（日历边框、时间标签）

**优先级**：🟡 中

---

### 19. 动态字体支持不完整

**当前状态**：使用系统字体样式，但部分固定尺寸可能被覆盖

**建议**：
- 测试辅助功能 - 更大字体（XXL）
- 确保关键内容（问题、答案、建议）不被截断
- 输入控件高度应响应字体大小

**优先级**：🟡 中

---

## 八、性能与细节优化

### 20. 🔴 ScrollView 内嵌套过深

**当前状态**：`TodayView` 使用 `ScrollView > VStack > Section(VStack) > ...`

**问题**：过多嵌套可能影响滚动性能

**建议**：考虑使用 `List` 或 `LazyVStack` 优化

**优先级**：🔴 高

---

### 21. 日历网格使用 `LazyVGrid` 但数据量小

**当前状态**：日历只显示一个月（~42 个单元格）

**建议**：直接使用 `VStack` + `HStack` 可能更简单高效

**优先级**：🟢 低

---

### 22. 🔴 图片资源未使用 SF Symbols 变体

**当前状态**：所有图标使用基础 SF Symbols

**iOS 26/27 新特性**：SF Symbols 6 引入更多变体和动画

**建议**：
- 麦克风录音态考虑使用 `.symbolEffect(.pulse)` 动画
- 完成对勾使用 `.symbolEffect(.bounce)`

**优先级**：🔴 高

---

## 九、数据展示优化

### 23. 🔴 日期格式显示不一致

**当前状态**：
- 日历详情：`.headline.bold()`
- 列表项：`.caption` + `.dateTime.month().day()`

**建议**：统一日期格式化，使用 `DateExtensions` 统一管理

**优先级**：🔴 高

---

### 24. 🔴 空状态文案过于技术化

**当前状态**：
- "No Ask Record Yet"
- "No Stories Yet"

**建议**：
- 更情感化："Your journey begins here"
- 或更引导性："Ask your first question"

**优先级**：🔴 高

---

### 25. Refine 对比页面缺少字数统计

**当前状态**：只显示原文和优化版

**用户需求**：可能想知道内容长度变化

**建议**：顶部显示字数（如 "128 words → 145 words"）

**优先级**：🟢 低

---

## 十、特定页面优化

### 26. 🔴 Onboarding 缺少进度指示器

**当前状态**：只有"Next"按钮，用户不知道还有几步

**建议**：顶部添加页面指示器（3 个圆点）

**优先级**：🔴 高

---

### 27. CalendarSheet 缺少快速跳转

**当前状态**：只能逐月切换

**建议**：
- 点击月份标题弹出月份/年份选择器
- 或添加"今天"按钮

**优先级**：🟢 低

---

### 28. Settings 语言选项被禁用但仍显示

**当前状态**：`.disabled(true)` 灰色显示

**建议**：
- 完全隐藏未实现功能
- 或添加 "Coming soon" 标签

**优先级**：🟢 低

---

### 29. 🔴 Story 列表缺少年份分组

**当前状态**：平铺显示所有月份

**长期使用问题**：几年后列表会很长

**建议**：按年份分组，使用 Section 标题

**优先级**：🔴 高

---

### 30. 🔴 Ask 结果缺少时间戳

**当前状态**：只显示月/日

**问题**：同一天多次提问无法区分

**建议**：添加具体时间（如 "Aug 12, 2:30 PM"）

**优先级**：🔴 高

---

## 优先级分类

### 🔴 高优先级（影响核心体验）

1. ✅ 导航栏标题方式（已修复）
3. 卡片圆角统一
4. 空状态图标字重
5. 列表间距
6. 内容水平内边距
7. Section 标题间距
8. 行间距统一
9. 标签文字大小
10. 按钮文字统一
11. 输入框焦点反馈
14. 扩展 Liquid Glass 使用（Ask 结果、Story 卡片）
20. ScrollView 嵌套优化
22. SF Symbols 动画
23. 日期格式统一
24. 空状态文案优化
26. Onboarding 进度指示
29. Story 年份分组
30. Ask 结果时间戳

### 🟡 中优先级（提升品质感）

12. 按钮按下反馈
17. 无障碍标签
18. 颜色对比度验证
19. 动态字体支持

### 🟢 低优先级（长期优化）

2. TabBar 最小化行为（等待 API 恢复）
13. 长按手势
15. iOS 27 排版特性
16. 系统级颜色语义
21. 日历网格优化
25. Refine 字数统计
27. 日历快速跳转
28. Settings 未实现功能处理

---

## 修改建议总结

当前 Moi App 的设计整体上**遵循了 iOS 原生规范**，代码质量高，结构清晰。主要改进方向集中在：

1. **细节统一性**：圆角、间距、字体等设计 token 的系统化定义
2. **交互反馈**：增强视觉响应，提升操作确认感
3. **iOS 26/27 新特性**：更充分利用 Liquid Glass 和新动画
4. **无障碍完善**：补充标签，验证对比度
5. **长期可维护性**：分组、分页等为未来扩展预留空间

---

## 文档变更历史

| 版本 | 日期 | 修改内容 | 修改人 |
|-----|------|---------|--------|
| 1.0 | 2026-08-12 | 初始版本，基于完整视图分析 | Claude |
