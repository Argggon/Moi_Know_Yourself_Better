# Moi 产品需求文档（PRD）

> 文档版本：V3.0 Draft  
> 更新日期：2026-08-12  
> 文档状态：本地评审稿，确认后进入版本控制  
> 产品阶段：MVP 前端框架确认，核心业务与后端待产品化  
> 适用范围：iOS App、AI 编排服务、数据与运维后台

## 0. 文档说明

本文档是 Moi 后续产品、设计、iOS、后端、AI、测试与发布工作的主需求文档。它整合并替代早期《MVP 需求文档 V2.0》中与当前产品方向重叠或冲突的部分。

本文档遵循以下优先级：

1. 已由产品负责人确认的当前 UI 与交互，作为前端设计基线。
2. 原始需求中的核心产品价值与功能目标。
3. 已定义的 Moi AI Skill 规则、Prompt 和数据 Schema。
4. 本文档补全的后端、隐私、可靠性、测试和验收要求。

除非另有说明，文中的“必须”表示 MVP 发布阻塞项，“应”表示 MVP 目标项，“可”表示后续增强项。

### 0.1 当前验证状态

- 已验证：Xcode 27 beta 编译通过；iPhone 17 Pro、iOS 27 模拟器主要 UI 与导航交互通过人工验收。
- 未验证：iOS 27 真机、iOS 26 模拟器与真机、iOS 18 模拟器与真机。
- 未验证：完整 VoiceOver、动态字体、弱网、断网、跨时区、月末调度、长文本和中英双语流程。
- 因此，当前状态为“UI 基线可用”，不等同于“发布质量已验收”。

## 1. 产品概述

### 1.1 产品名称

**Moi**，法语意为“我”。暂不设中文品牌名，全球统一使用 Moi。

品牌名在 App Store、商标、域名和社交账号上的可用性仍需在正式发布前完成法律与市场核查。若存在同品类冲突，备用命名为 **Moi App** 或 **The Moi App**。

### 1.2 一句话定义

**Ask yourself one question a day, and discover who you truly are.**

每天回答一个问题，让日积月累的表达成为理解自己的材料。

### 1.3 核心隐喻

Moi 是一面镜子：不评判、不诊断、不说教，只帮助用户看见自己已经表达过的感受、价值观与选择模式。

### 1.4 产品形态

Moi 是一个轻量、非对话式的 AI 自我探索工具，而不是聊天机器人：

- 每天只提出一个经过控制的问题。
- 用户可随时记录自发想法。
- AI 主要负责润色、归纳、检索与映照，不持续追问。
- 核心输出是月度 Story 与基于个人历史的 Ask 决策指引。

### 1.5 目标平台与语言

- 平台：iPhone，最低支持 iOS 18。
- 重点适配：iOS 18、iOS 26、iOS 27。
- UI：SwiftUI，遵循各系统原生导航、Sheet、材质和 Liquid Glass 行为。
- 语言：英语、简体中文；界面、通知、问题和 AI 输出跟随 App 语言设置。
- iPad、macOS、Android、Web 不属于 MVP。

## 2. 产品目标与边界

### 2.1 用户问题

现有 AI 问答或日记工具存在以下问题：

- 问题重复或语义高度相似，破坏长期使用意愿。
- 深度失衡，连续沉重提问造成心理疲劳。
- AI 记忆不可见且不稳定，容易遗漏重要材料。
- 只依赖预制问题，无法吸收用户随时产生的表达。
- 通用聊天产品反馈过多，不适合“只想记录”的场景。
- 月度总结容易泛化、说教或凭空推断，缺乏用户原话证据。

### 2.2 MVP 产品目标

- 建立稳定的每日记录习惯，单次核心操作可在 30 秒至 3 分钟内完成。
- 在任意连续 7 个有效提问中，维持 Light : Medium : Deep 约 3 : 3 : 1。
- 尽可能避免语义重复，并为重复风险提供可观测、可重试的工程机制。
- 将每日回答与随手记沉淀为本地、可回溯的数据资产。
- 每月生成有用户原话依据、不过度推断的个人 Story。
- Ask 必须基于用户历史给出明确但非强制的单轮立场。
- 默认保护隐私：原始个人文本不作为服务端长期数据保存，不用于模型训练。

### 2.3 非目标

- 不提供开放式连续聊天。
- 不提供心理疾病诊断、治疗建议或危机干预服务。
- 不承诺对人格、职业、关系或健康作出客观结论。
- MVP 不支持自定义模型或用户 API Key。
- MVP 不支持公开社区、关注、评论或内容广场。
- MVP 不支持数据导出和跨设备同步；但数据模型需为后续迁移预留稳定 ID 与版本。

## 3. 用户与核心场景

### 3.1 目标用户

- 希望形成轻量自我记录习惯，但不愿写长日记的人。
- 对情绪、环境和个人选择较敏感，重视自我理解的人。
- 希望 AI 使用自己的历史表达提供参考，而非泛泛回答的人。
- 偏好安静、原生、低干扰工具体验的 iPhone 用户。

### 3.2 核心用户旅程

1. 用户首次启动，完成昵称、语言、提醒时间和 Story 日期设置。
2. 每日收到提醒，进入 Today 回答一个不重复且深度合适的问题。
3. 用户可接受轻度润色或保留原文，并在日历中长期回看。
4. 用户随时通过同一输入组件记录短暂想法。
5. 当用户遇到选择困难时，在 Ask 中输入问题，获得引用个人历史的单轮建议。
6. 到达月度生成日，系统整合记录、更新内部画像并生成 Story。
7. 用户通过 Story 回看自己的情绪气候、重复模式与原话。

## 4. 信息架构与设计基线

### 4.1 一级导航

底部仅保留三个 Tab：

- **Today**：每日问题、回答、随手记、日历入口。
- **Ask**：决策输入、生成结果、历史记录。
- **Story**：月度信件列表与阅读。

Settings 不占用 Tab，通过各根页面右上角头像进入 Sheet。

### 4.2 当前确认的视觉原则

- 安静、原生、工具化，不使用营销页式构图。
- 根页面标题位于导航栏左侧，与右侧按钮水平对齐。
- iOS 27 导航栏按原生滚动最小化行为出现和消失，不生成中央小标题过渡。
- iOS 26/27 使用系统 Liquid Glass；不得在已有玻璃容器内叠加第二层玻璃。
- iOS 18 使用系统 Material 或等价的非玻璃兼容样式。
- 输入组件由长椭圆输入玻璃与右侧独立圆形确认玻璃组成。
- 麦克风位于长椭圆内部左侧，不具有独立玻璃圆形背景。
- 默认麦克风使用 `primary` 前景色，随浅色/深色模式自动反色；录音态使用红色波形或麦克风。
- 所有 Sheet 使用右上角圆形 `xmark` 关闭按钮，不使用 `Done` 文字。
- 当前已确认 UI 不应因旧 PRD 的卡片、按钮或排版描述被回退。

### 4.3 响应与无障碍要求

- 所有可点击图标的有效点击区域不得小于 44 × 44 pt。
- 支持浅色、深色、高对比度和“减弱动态效果”。
- 支持 VoiceOver 标签、标题语义和合理的焦点顺序。
- 支持系统动态字体；超长文本不得覆盖、截断关键操作或改变固定控件尺寸。
- 键盘出现时输入与提交控件必须保持可见，支持键盘 Done 提交。

## 5. 功能需求

### 5.1 Onboarding

#### 5.1.1 流程

首次启动展示 3 步引导：

1. 欢迎与产品理念。
2. 昵称设置，可跳过，默认值为 `you`。
3. 偏好设置：App 语言、每日提醒时间、月度 Story 日期。

默认每日提醒时间为本地时间 08:00。默认 Story 日期为首次注册日的日数；用户应可调整为 1 至 28，以避免不同月份不存在该日期。若未来允许 29 至 31，必须定义短月回退规则。

#### 5.1.2 权限

- 通知权限仅在用户确认提醒设置时请求，不在欢迎页冷启动请求。
- 麦克风和语音识别权限仅在首次点击麦克风时请求。
- 权限被拒绝时，功能降级为文本输入，并提供进入系统设置的明确入口。

#### 5.1.3 验收标准

- 完成或跳过后进入 Today，重启不重复展示。
- 用户设置立即本地持久化并正确创建通知任务。
- 跳过操作采用默认值，且不造成空昵称或无效日期。

### 5.2 Today：每日问答

#### 5.2.1 每日问题

- 每个自然日按设备当前时区最多生成一个有效问题。
- 当天问题生成后本地缓存，普通刷新、重启和切换 Tab 不得改变。
- 问题同时保存中英文版本、深度、标签、语义指纹、生成版本和生成时间。
- 无网络时优先显示已缓存问题；若当天尚无问题，使用本地安全备用问题并标记来源。
- 调试版可保留手动重新生成入口；生产版默认隐藏。

#### 5.2.2 回答输入

- 支持文本和语音转写。
- 使用已确认的统一 Liquid Glass 输入组件。
- 空白或仅包含换行的内容不可提交。
- 提交期间防止重复请求；离开页面后任务状态可恢复或明确失败。
- 用户原始输入必须在发起润色请求前本地暂存，避免请求失败造成丢失。

#### 5.2.3 Refine

- 回答提交后默认执行轻度润色。
- 润色不得新增事实、改变立场、弱化或夸大情绪、加入诊断或建议。
- 处理状态使用低干扰进度提示。
- 请求失败、超时或离线时，用户仍可直接保存原文。
- 润色结果进入双页对比：`Your words` 与 `Refined`。
- 两页均允许直接编辑；最终选择保存原文或润色文。
- 必须同时保留原文、润色版本和最终选择，不覆盖原始数据。

#### 5.2.4 完成状态

- 保存后问题仍显示，回答以可读形式或摘要呈现，并显示完成对勾。
- 同一天默认不可创建第二条 DailyLog；允许在未来版本中增加编辑，但 MVP 历史日只读。
- Today 中的随手记不受“当天问题已完成”状态影响。

#### 5.2.5 日历

- 月视图为 7 列日期网格，支持前后月份切换。
- 区分未回答、已回答、今日和当前选中日期。
- 选中日期后展示当日问题、最终回答和全部随手记。
- 历史内容 MVP 只读。
- 日期匹配必须基于本地日历与时区处理，避免 UTC 跨日错误。

#### 5.2.6 验收标准

- 同一自然日只显示同一个问题。
- 原文在断网、后台切换和 AI 失败时不丢失。
- 用户明确选择后，DailyLog 才标记为完成。
- 日历可正确查询跨月记录，且不会因时区变化产生重复日期。

### 5.3 Spontaneous Note：随手记

- Today 页面提供常驻的统一输入组件。
- 支持文本和语音转写，交互与 Today/Ask 一致。
- 提交后立即本地保存时间与原始内容，不调用润色。
- 一天允许保存多条，日历按时间倒序展示。
- 随手记参与月度画像与 Story，但不参与每日问题完成率。
- 隐身模式开启时不保存，也不发送到服务端。

### 5.4 Ask：决策指引

#### 5.4.1 输入与生成

- 用户输入一个具体困境，支持文本和语音。
- Ask 是单轮观点合成，不进入连续对话。
- 空内容不可提交；生成期间防止重复请求。
- 每次请求应检索用户画像与相关历史原话，而不是把全部历史无差别发送给模型。

#### 5.4.2 输出要求

- 必须给出明确立场，如 `PROCEED`、`DEFER`、`DECLINE`、`PRIORITIZE_SELF`。
- 解释控制在 3 至 5 句。
- 正常情况下必须引用至少 2 条来自用户历史的证据，并保留来源日期或记录 ID。
- 当有效历史少于 2 条时，不得伪造证据；应明确说明个人记录仍较少，并基于现有材料降低结论强度。
- 反思文案由客户端或服务端模板硬编码，不交给模型自由生成：
  - EN: `If this doesn’t feel right, maybe you already know a different answer.`
  - ZH: `若觉此言不合心，或许你心中早有答案。`
- 输出不得包含医疗、法律、投资等专业结论；高风险主题必须展示边界提示。

#### 5.4.3 历史记录

- 成功生成后本地保存困境、立场、解释、证据引用、语言、模型版本和时间。
- Ask 页面展示历史列表，按时间倒序。
- 点击历史项以 Sheet 查看完整结果。
- MVP 不提供“有帮助/无帮助”评价和连续追问。

#### 5.4.4 商业限制

- 免费版目标为每自然月 3 次成功 Ask；失败请求不计次数。
- MVP 验证期可暂不启用付费墙，但用量模型与服务端接口必须预留。

### 5.5 Story：月度叙事

#### 5.5.1 生成周期

- 在用户设置的 Story 日期生成上一完整周期的 Story。
- 生成是幂等任务：同一用户、同一周期只生成一个正式版本；重试不得产生重复信件。
- iOS 本地通知仅负责提醒，不能被视为可靠后台计算调度器。
- 正式方案由后端任务调度触发，或由 App 在前台/后台唤醒后检测缺失任务并补偿执行。

#### 5.5.2 输入材料

- 周期内已完成 DailyLog 的最终答案。
- 周期内全部 SpontaneousNote。
- 上一版结构化 UserProfile。
- 昵称、语言、周期和回答天数。
- 原始问题与用户原话必须与模型生成推断明确区分。

#### 5.5.3 双阶段生成

阶段一：增量画像合并。

- 保留长期稳定信息，除非新证据明确冲突。
- 输出结构化 JSON，并递增 Profile 版本。
- 画像只描述观察到的倾向，不进行心理诊断。
- 每条重要新增结论应保留证据记录 ID，便于审计和 Ask 检索。

阶段二：月度信件。

- 三段结构：情绪天气、关键模式与原话、结尾寄语。
- 至少引用 1 至 2 条真实用户原话；不得编造。
- 不给出说教式建议，不将相关性表述为因果关系。
- 落款为 `Your Moi, [Month Year]`。

#### 5.5.4 长度规则

- 有效回答天数 ≥ 15：英文约 300 至 400 words；中文约 400 至 600 字。
- 7 至 14 天：英文约 150 至 250 words；中文约 250 至 400 字。
- 3 至 6 天：英文约 80 至 120 words；中文约 120 至 200 字。
- 少于 3 天：不强行生成画像结论；生成诚实的极短信件，说明本月记录较少。

#### 5.5.5 阅读与通知

- Story Tab 显示月度信件列表、周期、标题和开头摘要。
- 详情使用沉浸式滚动阅读，正文采用适合长文的系统字体；不添加复杂动画。
- Story 生成成功后发送本地通知；App 内列表同步更新。
- 调试版可保留手动生成按钮；生产版应移除或隐藏。

### 5.6 Settings

#### 5.6.1 设置项

- Account：昵称；账号入口仅预留，不在 MVP 启用。
- Preferences：语言、每日提醒时间、Story 日期。
- Privacy & Data：清除全部本地数据、隐身模式。
- About：版本号、隐私政策、用户协议。
- 账号体系上线后增加 Sign Out。

#### 5.6.2 清除数据

- 必须二次确认。
- 清除 SwiftData、画像文件、Story 文件、草稿、缓存、向量索引和本地用量记录。
- 取消所有待处理通知。
- 若后端持有匿名指纹或任务状态，应调用删除接口。
- 删除完成后回到 Onboarding，不能留下可恢复的个人内容。

#### 5.6.3 隐身模式

- 开启后 Today 回答和随手记不保存、不参与 Story、不进入 Ask 检索。
- UI 必须持续显示弱提示，避免用户误以为内容已保存。
- 可允许用户临时完成当次操作，但关闭页面后内容即丢弃。

## 6. AI 问题生成系统

### 6.1 深度定义

- **Light**：感官、天气、当下偏好，可在约 30 秒内回答。
- **Medium**：日常边界、工作、关系、微型选择和行为模式。
- **Deep**：身份、核心价值、重要记忆和哲学反思；不得连续出现。

### 6.2 3:3:1 调度

- 调度器读取最近 6 个有效问题，选择第 7 个深度，使滚动窗口尽量达到 3 Light、3 Medium、1 Deep。
- 历史不足 7 天时使用带约束的加权分配，首日默认 Light 或 Medium。
- Deep 后至少间隔 3 个非 Deep 问题。
- 用户连续跳过或低活跃时，不因补齐比例而集中推送 Deep。

### 6.3 去重管线

服务端必须采用多阶段去重，而不是只依赖 Prompt：

1. 规则过滤：最近 3 天不得重复主要标签；过滤禁用主题和相同句式模板。
2. 指纹召回：根据 `semantic_fingerprint` 与 embedding 召回历史近邻。
3. 相似度判定：与个人全部历史及近期全局样本比较；阈值由离线评估确定，不在 PRD 中写死。
4. 候选重试：不合格则在同一深度下重新生成，最多 3 次。
5. 安全回退：仍失败时使用经过人工审核的本地备用题，但不得与用户近期问题重复。

“零重复”是产品目标而非可数学保证的承诺。上线指标应使用语义重复率和用户换题率衡量。

### 6.4 情境注入

- MVP 必须注入日期、星期、语言和近期深度窗口。
- 天气与城市属于 Phase 2；只有获得用户授权且数据可信时注入。
- 情境只能用于 Light/Medium，自然融入问题，不得暴露精确位置或制造被监视感。

### 6.5 输出 Schema

每日问题接口必须返回：

```json
{
  "question_id": "q_20260812_<opaque>",
  "question_en": "...",
  "question_zh": "...",
  "depth_level": "light",
  "tags": ["sensory", "mood"],
  "semantic_fingerprint": "impact of current atmosphere on energy",
  "source": "generated",
  "prompt_version": "daily-question-v1"
}
```

客户端必须严格解码 Schema；字段缺失、枚举非法或内容为空时视为失败，不直接显示未经校验的模型文本。

## 7. 数据模型

### 7.1 本地数据为主

MVP 的个人内容以设备本地为权威数据源：SwiftData 保存结构化记录，文件存储保存 Story 正文及可迁移画像。服务端仅在请求生命周期内处理必要文本，除明确列出的匿名元数据外不长期保存正文。

### 7.2 核心实体

#### DailyLog

- `id: UUID`
- `localDate: String`，格式 `YYYY-MM-DD`，与时区共同保证日唯一性
- `createdAt`, `updatedAt`, `timezoneIdentifier`
- `questionId`, `questionEn`, `questionZh`
- `depthLevel`, `tags`, `semanticFingerprint`
- `rawAnswer`, `refinedAnswer`, `finalAnswer`
- `answerSelection: original | refined | edited`
- `status: draft | refining | review | completed | failed`
- `questionSource`, `promptVersion`, `modelVersion`

#### SpontaneousNote

- `id`, `createdAt`, `timezoneIdentifier`
- `content`
- `isPrivateSession`

#### AskRecord

- `id`, `createdAt`
- `dilemma`
- `stance`, `headlineZh`, `headlineEn`
- `explanationZh`, `explanationEn`
- `evidenceRefs[]`
- `promptVersion`, `modelVersion`

#### QuestionFingerprint

- `questionId`, `localDate`
- `questionZh`, `questionEn`
- `depthLevel`, `tags`
- `semanticFingerprint`
- 可选本地 embedding 或 embedding 版本，不保存到日志系统

#### UserProfile

- `version`, `lastUpdated`, `nickname`
- `coreTraits[]`, `coreValues[]`
- `emotionalPatterns`
- `behavioralObservations[]`, `growthMilestones[]`
- `evidenceRefs[]`
- `schemaVersion`, `promptVersion`

#### MonthlyStory

- `id`, `periodStart`, `periodEnd`, `generatedAt`
- `titleZh`, `titleEn`, `bodyZh`, `bodyEn`, `signOff`
- `answeredDays`, `sourceRecordIds[]`
- `profileVersion`, `promptVersion`, `modelVersion`
- `generationStatus`

### 7.3 数据迁移

- 所有实体必须包含稳定 ID 和 Schema 版本。
- SwiftData Schema 变更必须提供迁移计划与历史数据回归测试。
- 当前 Markdown 画像应迁移到结构化 JSON；Markdown 可作为可读镜像，不应继续作为唯一机器数据源。
- Story 文件名应改用稳定周期键，如 `Story_2026-08.md`，展示标题与文件键分离。

## 8. 后端与 AI 架构

### 8.1 架构原则

- App 不得内置第三方模型 API Key，也不得在生产环境直连模型提供商。
- 所有 AI 请求通过 Moi API，由服务端处理鉴权、限流、Schema 校验、Prompt 版本和模型路由。
- 服务端不把用户正文写入应用日志、错误追踪、分析平台或数据库。
- Prompt 和模型版本可灰度、可回滚、可审计。

### 8.2 推荐组件

- API：FastAPI 或同等级异步框架。
- 数据库：PostgreSQL，保存匿名设备主体、用量、任务状态、指纹元数据和版本信息。
- 向量检索：MVP 可使用 pgvector；个人正文 embedding 是否上云需单独隐私评审。默认优先客户端召回并仅上传选中的必要文本。
- 队列：Redis + worker 或托管队列，用于 Story 生成、重试和限流。
- 对象存储：MVP 不存用户 Story 正文；未来同步需端到端加密方案评审。
- 可观测性：结构化无正文日志、指标、Trace ID 和错误码。

### 8.3 API 草案

所有接口使用 HTTPS、JSON、`X-Request-ID` 和匿名安装令牌。写接口支持 `Idempotency-Key`。

#### `POST /v1/questions/daily`

输入：本地日期、时区、语言、最近深度、历史标签和语义指纹摘要。  
输出：第 6.5 节的严格 Schema。  
服务端：调度深度、生成候选、多阶段去重、返回最终题。

#### `POST /v1/answers/refine`

输入：问题、原始回答、语言。  
输出：`refined_answer`、`prompt_version`、`model_version`。  
约束：不持久化正文；超时目标 15 秒；支持客户端取消。

#### `POST /v1/ask/guidance`

输入：困境、结构化画像、最多 N 条客户端筛选的相关原话。  
输出：双语标题、标准 stance、解释、证据引用和固定反思文案。  
约束：服务端校验证据是否来自输入，不允许模型返回未知引用。

#### `POST /v1/stories/generate`

输入：周期、昵称、结构化画像、周期记录及幂等键。  
输出：任务 ID；小数据量可同步，正式环境推荐异步。

#### `GET /v1/tasks/{task_id}`

输出：`queued | running | succeeded | failed`、可重试错误码和成功结果。不得在响应之外持久暴露原始输入。

#### `DELETE /v1/installations/{installation_id}`

删除匿名安装主体、指纹元数据、用量和未完成任务。客户端“清除全部数据”时调用。

### 8.4 鉴权与匿名身份

- 首次安装由服务端签发随机 installation ID 与短期访问令牌。
- 不直接使用 IDFV 作为数据库主键；IDFV 只可用于本地迁移辅助，避免跨 App 追踪风险。
- 令牌保存在 Keychain。
- 账号与 CloudKit 同步属于 Phase 2，接入前重新设计身份合并和删除流程。

### 8.5 错误模型

```json
{
  "error": {
    "code": "MODEL_TIMEOUT",
    "message": "The request could not be completed.",
    "retryable": true,
    "request_id": "req_..."
  }
}
```

客户端不得直接展示模型供应商错误。可重试错误采用指数退避并限制次数；非可重试错误提供安全降级路径。

## 9. 隐私、安全与内容边界

### 9.1 数据分类

- 高敏感：DailyLog、随手记、Ask 困境、画像、Story、语音转写。
- 中敏感：问题历史、标签、语义指纹、使用次数。
- 低敏感：无内容的性能指标、版本、错误码。

### 9.2 必须遵守

- 生产日志不得记录请求/响应正文、Authorization、用户原话或画像。
- 模型供应商配置必须关闭训练用途，并核对数据保留条款。
- 传输使用 TLS；本地依赖 iOS Data Protection，令牌使用 Keychain。
- 崩溃报告和分析事件只使用枚举与计数，不包含自由文本。
- 隐私政策必须准确说明 AI 文本会被临时发送至服务端及模型供应商，不能笼统宣称“100% 只在本地”。
- 用户删除操作必须可验证、可重试，并定义服务端删除完成状态。

### 9.3 安全与伦理边界

- Moi 不作心理诊断，不替代专业服务。
- 对自伤、伤害他人、虐待、紧急医疗等高风险表达，Ask 不应给普通决策立场；应返回安全边界文案和地区化求助建议。
- 对法律、医疗、金融等高风险领域，输出必须降低确定性并提示咨询专业人士。
- 不根据种族、宗教、性别、性取向、残障等敏感属性作歧视性推断。

## 10. 通知与任务调度

- 每日问题提醒为本地重复通知，文案跟随语言。
- Story 通知只能在 Story 已成功生成或可立即生成时出现，不得提前声称“已生成”。
- 修改时间后必须取消旧通知并重建。
- 时区变化、夏令时和系统重启后必须重新校验下一次触发时间。
- App 启动、回到前台和后台刷新时检查缺失的每日问题与 Story 任务。
- 后台能力不可用时，用户打开 App 后执行补偿生成，并清楚显示状态。

## 11. 本地化

- UI 字符串使用 String Catalog，不在 View 中散落硬编码语言判断。
- 日期、数字、月份和通知遵循 Locale。
- AI 接口返回中英双语，客户端按当前语言展示并保留另一语言以支持切换。
- 中文和英文 Prompt 使用相同的产品约束与 Schema，不以简单翻译替代独立质量评估。
- 发布前由母语审校检查所有高频流程和固定反思文案。

## 12. 分析指标与质量监控

### 12.1 产品指标

- Onboarding 完成率。
- 每日问题打开率、回答开始率、回答完成率。
- D1、D7、D30 留存。
- 平均每周回答天数与随手记数量。
- Refined 使用率与手工编辑率。
- Ask 发起、成功和历史回看率。
- Story 生成成功率、打开率和阅读完成代理指标。

### 12.2 AI 质量指标

- 每日问题语义重复率。
- 深度比例偏差和 Deep 连续出现率。
- 问题重新生成率、备用题使用率。
- Refine 事实改变率、用户回退原文率。
- Ask 有效证据覆盖率、伪引用率（目标为 0）。
- Story 原话准确率、Schema 解析成功率和人工质量评分。

### 12.3 隐私约束

分析事件不得携带问题、答案、困境、Story、昵称或其他自由文本。安装标识应可重置，并遵循用户删除请求。

## 13. 非功能需求

### 13.1 性能

- 冷启动至可交互目标：中位数 < 1.5 秒，P95 < 2.5 秒（目标设备另行定义）。
- 本地页面切换与滚动保持 60 fps，长文本不造成明显主线程阻塞。
- 本地保存操作目标 < 200 ms。
- AI 请求：Refine/Ask P95 < 15 秒；Story 可异步但应持续显示状态。

### 13.2 可靠性

- 所有写入先本地、后联网；联网失败不得导致用户文本丢失。
- 网络请求支持取消、超时、幂等和有限重试。
- Story 任务可从中断状态恢复。
- 服务端月可用性目标 99.5%（MVP），错误率和模型供应商状态可观测。

### 13.3 可维护性

- 前端按 View、Domain、Repository、API Client 分层，避免业务逻辑集中在 View。
- 使用 Codable DTO 严格解析 API，不使用自由字典。
- Prompt、Schema、模型和客户端最低版本均需版本化。
- 提供开发、预发布、生产三套环境；密钥只存在服务端 Secret 管理中。

## 14. 测试与发布验收

### 14.1 平台矩阵

发布前至少覆盖：

- iOS 18：最小尺寸支持机型、主流非 Pro 机型、真机至少 1 台。
- iOS 26：模拟器与真机至少各 1 台。
- iOS 27：iPhone 17 Pro 模拟器与真机至少 1 台。
- 浅色、深色、动态字体最大辅助级别、VoiceOver、减弱动态效果。

### 14.2 核心端到端场景

- 首次安装、跳过与完整 Onboarding。
- 通知授权允许/拒绝/后续修改。
- 每日问题在线生成、缓存、离线回退和跨日。
- 文本/语音回答、AI 成功/失败、原文/润色选择。
- 随手记多条保存与日历回看。
- Ask 正常历史、历史不足、高风险主题和失败重试。
- Story 各回答天数区间、幂等重试、月末与跨时区。
- 清除全部数据后无残留并回到 Onboarding。
- 中英切换后 UI、通知、历史展示和新 AI 输出一致。

### 14.3 当前 UI 回归重点

- 根页面标题与右侧按钮水平对齐。
- 下拉时标题不随内容拉伸；上滑恢复时不出现中央小标题。
- iOS 27 导航栏按系统行为最小化与恢复。
- 输入胶囊与确认圆钮尺寸协调，麦克风无玻璃叠加。
- Sheet 关闭按钮只有一层玻璃且保持圆形。
- iOS 18 不调用 iOS 26/27 API，兼容样式可操作且无布局退化。

## 15. MVP 范围与里程碑

### Phase 0：已完成基线

- 三 Tab 与 Settings Sheet 框架。
- iOS 27 原生导航栏行为。
- 统一 Liquid Glass 输入组件及 iOS 18 兼容分支。
- Sheet `xmark` 关闭按钮。
- Today、Ask、Story、日历、Onboarding 的可运行前端原型。
- SwiftData、Speech、本地通知和本地画像/Story 原型。

### Phase 1：MVP 发布阻塞

- 正式后端 API 与匿名鉴权。
- 每日问题 3:3:1 调度和多阶段去重。
- Refine、Ask、Story 严格 Schema 与可靠性处理。
- 本地数据模型升级与迁移。
- 完整 Settings、隐身模式、通知重建和删除闭环。
- 中英双语与 String Catalog。
- 隐私政策、用户协议和高风险内容处理。
- iOS 18/26/27 与真机测试矩阵。

### Phase 2：快速迭代

- 天气与粗粒度位置情境问题。
- CloudKit 加密同步与账号体系。
- Ask 订阅与用量控制。
- 分享卡片、iPad 适配、数据导出。
- 更成熟的端侧向量检索与个性化调度。

## 16. 商业化

- 免费：每日问答、随手记、历史日历；Ask 每月目标 3 次。
- Moi Plus：无限 Ask、自动 Story 及未来高级个性化。
- 首次公开版本可暂不收费，以验证留存、问题质量和 Story 价值。
- 付费墙不得阻断用户访问自己已经生成的本地内容。

## 17. 待决策事项

以下事项必须在对应开发开始前由产品、技术和隐私共同确认：

1. 品牌名 Moi 的商标与 App Store 同品类冲突风险。
2. 正式模型供应商、数据保留条款、区域部署与成本上限。
3. 语义去重由客户端、服务端或混合执行，以及 embedding 是否允许上云。
4. Story 的可靠生成机制：服务端定时任务、App 补偿任务或两者结合。
5. 隐身模式是否允许临时调用 AI；默认建议完全不发送。
6. 少于 2 条历史证据时 Ask 的具体降级文案。
7. 免费 Ask 次数和 Moi Plus 上线时点。
8. iOS 18 的最终兼容视觉是否接受 Material，或需单独设计。
9. MVP 是否允许用户编辑已完成的当日回答。
10. 数据导出是否因隐私法规或 App Store 审核要求提前进入 MVP。

## 18. 发布定义（Definition of Done）

MVP 只有在以下条件全部满足时才可提交 App Store：

- Phase 1 发布阻塞功能全部完成，并有对应自动化或人工验收记录。
- iOS 18、26、27 的模拟器与真机测试通过，无阻塞级 UI 或数据问题。
- AI 输出 Schema、去重、引用真实性和安全边界达到约定阈值。
- 弱网、断网、超时和模型故障不会导致用户文本丢失。
- 隐私政策与真实数据流一致，生产日志抽检不含个人正文。
- 清除数据流程覆盖本地与服务端并通过验证。
- App Store 隐私清单、权限用途文案、用户协议和支持页面完备。
- 产品负责人完成最终 UI、核心旅程和中英文内容验收。

---

## 附录 A：当前代码实现与目标差距

| 领域 | 当前实现 | 目标状态 |
|---|---|---|
| UI 框架 | 三 Tab、Settings、原生导航、统一输入已可用 | 保持视觉基线，补齐无障碍与多系统回归 |
| 本地数据 | SwiftData 四个模型 + Markdown 文件 | 增加状态、版本、证据引用、迁移和稳定周期键 |
| AI 接入 | 客户端 `LLMService` 可直连模型，空 Key 使用 Mock | App 仅调用 Moi API，生产 Key 只在服务端 |
| 每日去重 | 标签与 Prompt 约束原型 | 3:3:1 调度 + embedding 召回 + 阈值判定 + 重试 |
| Refine | 基础调用和双页对比 | 严格事实保持、草稿恢复、错误降级和版本记录 |
| Ask | 画像 + 少量历史文本生成 | 客户端检索、证据 ID、严格 Schema、高风险分流 |
| Story | 手动触发、Markdown 保存 | 幂等月度任务、结构化画像、动态长度、可靠通知 |
| 通知 | 基础本地通知 | 设置联动、语言、时区、补偿任务和状态校验 |
| 设置 | 昵称、禁用语言项、清除数据原型 | 提醒、Story 日、语言、隐身、政策和完整删除 |
| 隐私 | 设计意图存在 | 正式数据流、无正文日志、供应商条款与删除验证 |

## 附录 B：需求追踪约定

后续开发任务应引用本 PRD 的章节编号，并在 Issue/PR 中记录：

- 对应需求章节。
- 实现范围与不包含范围。
- 数据或 API 变更。
- 测试平台与结果。
- 是否涉及隐私、Prompt 或 Schema 版本变化。

当实现与本文档发生冲突时，应先更新 PRD 并经产品负责人确认，再合并代码。
